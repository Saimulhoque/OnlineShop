<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.forbit.onlineshop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignUpActivity"></activity>
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.AccountInformationActivity" />
        <activity android:name=".ui.ElectronicsDeviceActivity" />
        <activity android:name=".ui.WomenActivity" />
        <activity android:name=".ui.MenActivity" />
        <activity
            android:name=".ui.login.MenActivity"
            android:label="@string/title_activity_men" />
        <activity android:name=".ui.SettingsActivity" />
        <activity android:name=".ui.MessageActivity" />
        <activity android:name=".ui.CartActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>